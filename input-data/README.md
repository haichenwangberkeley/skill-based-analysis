This directory contains external data links and should not be tracked in git.

The repository's .gitignore excludes everything under `input-data/` so that large datasets or external file links are not added to the repository.

If you need to provide small example fixtures for tests, add them under `tests/fixtures/` or create a separate `data-sample/` directory and add it explicitly to the repo.
